---
title: 'Steps'
description: 'The Steps component is designed to visually represent a multi-step process. It supports both horizontal and vertical layouts, making it perfect for onboarding, wizards, and process visualizations.'
date: 2025-03-12
---

# Steps Component Documentation

The **Steps** component provides a structured way to represent a multi-step process. It supports both horizontal and vertical layouts, making it ideal for onboarding flows, wizards, and process visualizations.

## Preview and Code Snippet

<Tabs defaultValue="preview" size="sm" className="w-full">
  <TabsList className="grid w-full md:w-[30%] grid-cols-2">
    <Tab value="preview">Preview</Tab>
    <Tab value="code">Code</Tab>
  </TabsList>
  <TabsContent value="preview">
    <Preview>
      <Steps>
        <Step>
          <StepTitle>Step 1: Fork the Repository</StepTitle>
          <StepContent>
            <div>
              <p>Visit our GitHub repository and click the "Fork" button to create your own copy.</p>
              <a href="https://github.com/your-repo/example" target="_blank" rel="noopener noreferrer">Fork the Repository on GitHub</a>
            </div>
          </StepContent>
        </Step>
        <Step>
          <StepTitle>Step 2: Clone the Repository</StepTitle>
          <StepContent>
            <p>Use the following command to clone the repository to your local machine:</p>
            <pre><code>git clone https://github.com/your-repo/example.git</code></pre>
          </StepContent>
        </Step>
        <Step>
          <StepTitle>Step 3: Install Dependencies</StepTitle>
          <StepContent>
            <p>Navigate into the project directory and run:</p>
            <pre><code>npm install</code></pre>
          </StepContent>
        </Step>
      </Steps>
    </Preview>
  </TabsContent>
  <TabsContent value="code">

```tsx
import { Step, StepContent, Steps, StepTitle } from '@/components/step';

export default function Example() {
  return (
    <Steps>
      <Step>
        <StepTitle>Step 1: Fork the Repository</StepTitle>
        <StepContent>
          <p>
            Visit our GitHub repository and click the Fork button to create your
            own copy.
          </p>
        </StepContent>
      </Step>
      <Step>
        <StepTitle>Step 2: Clone the Repository</StepTitle>
        <StepContent>Clone the repository to your local machine.</StepContent>
      </Step>
      <Step>
        <StepTitle>Step 3: Install Dependencies</StepTitle>
        <StepContent>Run your package manager's install command.</StepContent>
      </Step>
    </Steps>
  );
}
```

  </TabsContent>
</Tabs>

## Nested Steps

Place another `Steps` group inside `StepContent` when a stage has its own ordered sub-process. The nested group starts its numbering at one while the outer sequence continues independently.

<Tabs defaultValue="preview" size="sm" className="w-full">
  <TabsList className="grid w-full grid-cols-2 md:w-[30%]">
    <Tab value="preview">Preview</Tab>
    <Tab value="code">Code</Tab>
  </TabsList>
  <TabsContent value="preview">
    <Preview>
      <Steps>
        <Step>
          <StepTitle>Prepare the project</StepTitle>
          <StepContent>
            <p>Complete these setup tasks before building:</p>
            <Steps className="mt-6">
              <Step>
                <StepTitle>Install dependencies</StepTitle>
                <StepContent>Run your package manager's install command.</StepContent>
              </Step>
              <Step>
                <StepTitle>Configure the environment</StepTitle>
                <StepContent>Copy the example environment file and review its values.</StepContent>
              </Step>
            </Steps>
          </StepContent>
        </Step>
        <Step>
          <StepTitle>Start development</StepTitle>
          <StepContent>Run the development server and open the application.</StepContent>
        </Step>
      </Steps>
    </Preview>
  </TabsContent>
  <TabsContent value="code">

```tsx
import { Step, StepContent, Steps, StepTitle } from '@/components/step';

export default function NestedStepsExample() {
  return (
    <Steps>
      <Step>
        <StepTitle>Prepare the project</StepTitle>
        <StepContent>
          <p>Complete these setup tasks before building:</p>
          <Steps className="mt-6">
            <Step>
              <StepTitle>Install dependencies</StepTitle>
              <StepContent>
                Run your package manager's install command.
              </StepContent>
            </Step>
            <Step>
              <StepTitle>Configure the environment</StepTitle>
              <StepContent>
                Copy the example environment file and review its values.
              </StepContent>
            </Step>
          </Steps>
        </StepContent>
      </Step>
      <Step>
        <StepTitle>Start development</StepTitle>
        <StepContent>
          Run the development server and open the application.
        </StepContent>
      </Step>
    </Steps>
  );
}
```

  </TabsContent>
</Tabs>

## Props

| Prop               | Type                 | Default   | Description                                                |
| ------------------ | -------------------- | --------- | ---------------------------------------------------------- |
| `contentPosition`  | `"right" \| "below"` | `"below"` | Defines whether the content appears to the right or below. |
| `className`        | `string`             | `""`      | Custom class for styling the steps container.              |
| `titleClassName`   | `string`             | `""`      | Custom class for styling the step title.                   |
| `contentClassName` | `string`             | `""`      | Custom class for styling the step content.                 |
| `lineClassName`    | `string`             | `""`      | Custom class for styling the connector line.               |
| `numberClassName`  | `string`             | `""`      | Custom class for styling the step number circle.           |

The **Steps** component is highly flexible and allows for styling customization to fit various UI requirements.
